@* =============================================================================
    فایل Pages/NotFound.cshtml — صفحه "محصول یافت نشد"
    =============================================================================
    @page "/noid": مسیر سفارشی — از /noid قابل دسترسی است.
    الگوی @functions: PageModel مستقیماً در فایل View تعریف شده.
    ============================================================================= *@

@page "/noid"
@model NotFoundModel
@using Microsoft.AspNetCore.Mvc.RazorPages
@using WebApp.Models;

<!DOCTYPE html>
<html>
<head>
    <link href="/lib/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
    <title>Not Found</title>
</head>
<body>
    <div class="bg-primary text-white text-center m-2 p-2">No Matching ID</div>
    @* نمایش لیست همه محصولات به عنوان جایگزین *@
    <ul class="list-group m-2">
        @foreach (Product p in Model.Products) {
            <li class="list-group-item">@p.Name (ID: @p.ProductId)</li>                
        }
    </ul>    
</body>
</html>

@functions {
    public class NotFoundModel: PageModel {
        private DataContext context;

        // لیست محصولات برای نمایش به کاربر
        public IEnumerable<Product> Products { get; set; } 
            = Enumerable.Empty<Product>();

        public NotFoundModel(DataContext ctx) {
            context = ctx;
        }

        // بارگذاری همه محصولات
        public void OnGetAsync(long id = 1) {
            Products = context.Products;
        }
    }
}
